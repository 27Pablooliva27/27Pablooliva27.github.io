export enum Tipo {
    CADENA,
    ENTERO,
    DECIMAL,
    CHAR,
    BOOL,
    POSITION,
    LAST,
    IDENTIFICADOR,
    NULL,
    VOID,
    INT_FUNCTION,
    DOUBLE_FUNCTION,
    STRING_FUNCTION,
    CHAR_FUNCTION,
    BOOLEAN_FUNCTION,
    IF,
    WHILE,
    DO_WHILE,
    SWITCH,
    FOR,
    ATRIBUTO,
    ARRAY,
    STRUCT,
    PRINT,
    BREAK,
    CONTINUE,
    RETURN
}